# Open Timeline Engine Git Capture

## Commands

- `tce-git-capture install --repo .`
- `tce-git-capture capture-commit --repo .`
- `tce-git-capture capture-prepush --repo .`
- `tce-git-capture replay`

Full diff capture is disabled by default.
